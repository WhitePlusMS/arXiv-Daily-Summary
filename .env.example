# ============================================
# ArXiv 每日论文推荐系统 - 环境变量配置模板
# ============================================
# 说明：首次启动时，系统会自动从本文件创建 .env 文件
# 您可以通过前端界面的"环境配置"页面来修改这些配置
# ============================================

# ============================================
# 🤖 模型与 API 配置
# ============================================

# DashScope API 配置（必填）
# 请访问 https://console.aliyun.com/dashscope 获取您的 API 密钥
DASHSCOPE_API_KEY=YOUR_DASHSCOPE_API_KEY
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen3-235b-a22b-instruct-2507
QWEN_MODEL_LIGHT=qwen3-30b-a3b-instruct-2507
LIGHT_MODEL_PROVIDER=dashscope
HEAVY_MODEL_PROVIDER=dashscope

# ==================== ArXiv获取器配置 ====================
# ArXiv API基础URL
ARXIV_BASE_URL=http://export.arxiv.org/api/query
# 请求失败重试次数
ARXIV_RETRIES=3
# 请求间隔延迟（秒）
ARXIV_DELAY=5
# 每个分类最大获取条目数
MAX_ENTRIES=50
# 详细分析论文数
NUM_DETAILED_PAPERS=1
# 简要分析论文数
NUM_BRIEF_PAPERS=1

# ==================== LLM配置 ====================
# 重型通义千问模型（Qwen）常用参数
QWEN_MODEL_TEMPERATURE=0.7
QWEN_MODEL_TOP_P=0.9
QWEN_MODEL_MAX_TOKENS=4000

# 轻量通义千问模型（Qwen Light）常用参数
QWEN_MODEL_LIGHT_TEMPERATURE=0
QWEN_MODEL_LIGHT_TOP_P=0.8
QWEN_MODEL_LIGHT_MAX_TOKENS=2000

# 最大并发工作线程数
MAX_WORKERS=2

# ==================== 文件路径配置 ====================
# 研究兴趣描述文件路径
USER_CATEGORIES_FILE=data/users/user_categories.json
# 保存目录
SAVE_DIRECTORY=./arxiv_history
# 是否保存为Markdown格式 (true/false)
SAVE_MARKDOWN=true

# ==================== 邮件发送配置 ====================
# 是否启用邮件发送 (true/false)
SEND_EMAIL=false
# 发送者邮箱
SENDER_EMAIL=sender@example.com
# 接收者邮箱
RECEIVER_EMAIL=recipient@example.com
# 邮箱密码或应用专用密码
EMAIL_PASSWORD=your_email_password
# SMTP服务器地址
SMTP_SERVER=smtp.example.com
# SMTP端口
SMTP_PORT=587
# 是否使用SSL (true/false)
USE_SSL=false
# 是否使用TLS (true/false)
USE_TLS=true
# 邮件主题前缀
SUBJECT_PREFIX=[ArXiv每日推荐]

# ==================== 时区和格式配置 ====================
# 时区设置
TIMEZONE=Asia/Shanghai
# 日期格式
DATE_FORMAT=%Y-%m-%d
# 时间格式
TIME_FORMAT=%H:%M:%S
# 是否启用MCP时间服务 (true/false)
# 关闭时将直接使用Python本地时间，避免LLM调用开销
ENABLE_MCP_TIME_SERVICE=false

# ==================== 调试模式配置 ====================
# 是否启用调试模式 (true/false)
# 调试模式下将使用假数据，不会真实调用ArXiv API和LLM
DEBUG_MODE=false

# ==================== 日志配置 ====================
# 是否启用控制台日志 (true/false)
LOG_TO_CONSOLE=true
# 日志文件最大大小（MB）
LOG_MAX_SIZE=10
# 保留的日志文件数量
LOG_BACKUP_COUNT=5

# 相关性评分过滤阈值（低于该分数的论文将被剔除，0–10）
RELEVANCE_FILTER_THRESHOLD=6
